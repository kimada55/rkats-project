<%= form_for(@groups_val) do |f| %>
  <% if @groups_val.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@groups_val.errors.count, "error") %> prohibited this groups_val from being saved:</h2>

      <ul>
      <% @groups_val.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :type %><br />
    <%= f.text_field :type %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
